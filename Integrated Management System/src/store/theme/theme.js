const theme = {
    state: {
        primary: '#027be3',
        info:'#05a9f3'
    },
    mutations: {
        SET_PRIMARY(state, color) {
            state.primary = color
        },
        SET_INFO(state, color) {
            state.info = color
        }
    },
    actions: {
 
    }
};

export default theme;